<textarea
    {% if placeholder %}placeholder="{{ placeholder }}"{% endif %}
    {% if invalid %}aria-invalid="true"{% endif %}
    {% if disabled %}disabled data-ui-state="disabled"{% elseif invalid %}data-ui-state="invalid"{% endif %}
    {{ attributes }}
>{% block content %}{% endblock %}</textarea>
